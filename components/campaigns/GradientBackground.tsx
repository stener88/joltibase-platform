'use client';

/**
 * Subtle Gradient Background
 * Very transparent gradient for visual depth while maintaining readability
 */
export function GradientBackground() {
  return (
    <div 
      className="fixed inset-0 -z-10 bg-[#F5F5F5]"
    />
  );
}
